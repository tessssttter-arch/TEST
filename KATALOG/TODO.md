# Sync Hub: KATALOG

Детальный план задач для модуля KATALOG (Каталог товаров).

## Активные задачи
- [ ] Определение структуры данных каталога и импорта JSON (базовый контракт)
- [ ] Интеграция с локальной базой данных (`catalog-db.ts`)
- [ ] Оптимизация производительности поиска и фильтрации товаров
- [x] **[AI-EDIT] Контрольная проверка и доработка `src/services/product-normalizer.ts` | 2026-06-14**
  - Проверить логику функции `normalizeProduct` в отношении `product_id`.
  - Убедиться, что `raw.product_id` сохраняется, а `crypto.randomUUID()` генерируется только при отсутствии идентификатора.
  - Цель: предотвратить дублирование товаров при слиянии (merge) импортированных из VK постов.

## Завершенные задачи
*Пока нет завершенных задач. Все задачи выполняются строго с метками `[AI-EDIT]`.*
